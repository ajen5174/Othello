<Page
    x:Class="Othello.MainMenu"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Othello"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.5*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="OTHELLO" FontSize="72" TextAlignment="Center" VerticalAlignment="Center" CharacterSpacing="250" Grid.Column="1"/>

        <Button x:Name="PlayButton" Content="PLAY" FontSize="64" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="50" Grid.Column="1" Grid.Row="1" Width="200" Click="PlayButton_Click"/>
        <Button x:Name="ExitButton" Content="EXIT" FontSize="64" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="50" Grid.Column="1" Grid.Row="2" Width="200"  Click="ExitButton_Click"/>
    </Grid>
</Page>

